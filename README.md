# Family Geneology

Desktop genealogy app for Windows and macOS.

## Development (macOS)

```bash
npm install
npm run dev
```

## Build

```bash
npm run build:mac
npm run build:win
```

## Project format

- Working copy: folder with `family.sqlite`, `media/`, `project.json`
- Portable archive: `.fgtree` (ZIP64)
